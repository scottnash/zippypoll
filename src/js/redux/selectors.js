import * as c from './constants';

export const getZippyPollCreationFormValues = (state) => state.form.ZippyPollCreationForm && state.form.ZippyPollCreationForm.values ? state.form.ZippyPollCreationForm.values : { };
